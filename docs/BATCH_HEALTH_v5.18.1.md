# Batch health — v5.18.1

- dodati indikatori kvaliteta (OK/WARN/BAD) za batch-eve
- prikaz na Početnoj (dashboard) i u Batch detail panelima
- metrika: invalid JSON, missing fields, min steps, min ingredients, empty kolicina, duplikati unutar batch-a
- API: `/api/pipeline/health_summary` + proširen `/api/pipeline/batch_detail`

Server compile: OK
